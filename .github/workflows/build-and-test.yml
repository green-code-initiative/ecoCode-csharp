# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json

name: Build and Test

env:
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 1
  DOTNET_NOLOGO: true

on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
  pull_request:

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up .NET
      uses: actions/setup-dotnet@v4

    - name: Restore dependencies
      run: dotnet restore NoVsix.slnf

    - name: Build
      run: dotnet build NoVsix.slnf -c Release --no-restore

    - name: Pack # Check that packing the project works
      run: dotnet pack NoVsix.slnf -c Release --no-build

    - name: Run tests
      run: dotnet test NoVsix.slnf -c Release --no-build --logger "trx;LogFileName=test_results.trx"

    - name: Upload test results
      uses: actions/upload-artifact@v4
      if: always() # Ensure this step runs even if the test step fails
      with:
        name: Test Results
        path: '**/test_results.trx'